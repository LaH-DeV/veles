extern fn :: log(i32 value)
use math::constants

pub fn :: main {
	let bool test = true
	let i32 result = 2 * 8 + 1
	if test {
		log(result * constants::PI)
	}
}

pub fn i32 :: add(i32 a, i32 b) {
	return a + b
}